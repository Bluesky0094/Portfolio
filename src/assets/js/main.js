(function(){const body=document.body;const projects=window.PORTFOLIO_PROJECTS||{};function nav(){const toggle=document.querySelector(".nav-toggle"),menu=document.querySelector(".site-nav");toggle?.addEventListener("click",()=>{const open=menu.classList.toggle("open");body.classList.toggle("nav-open",open);toggle.setAttribute("aria-expanded",String(open));});document.querySelectorAll("[data-page-link]").forEach(a=>a.classList.toggle("active",a.dataset.pageLink===body.dataset.page));}function reveal(){const nodes=document.querySelectorAll(".reveal");if(matchMedia("(prefers-reduced-motion: reduce)").matches){nodes.forEach(n=>n.classList.add("visible"));return}const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");observer.unobserve(e.target)}}),{threshold:.12});nodes.forEach(n=>observer.observe(n));}function renderCase(){const root=document.querySelector("[data-case]");if(!root)return;const slug=body.dataset.project,p=projects[slug];if(!p)return;document.title=`${p.title} — Stefano Caccamo`;root.innerHTML=`<section class="case-hero"><div class="case-meta"><span>${p.type}</span><span>${p.year}</span></div><h1>${p.title}</h1></section><img class="case-cover" src="../../assets/img/optimized/${p.cover}" alt="${p.title}" fetchpriority="high"><section class="case-story"><p>Case study / ${p.year}</p><div class="story-blocks"><section><h2>Contesto</h2><p>${p.challenge}</p></section><section><h2>Approccio</h2><p>${p.approach}</p></section><section><h2>Risultato</h2><p>${p.outcome}</p></section></div></section><section class="case-gallery"><img src="../../assets/img/optimized/${p.cover}" alt="Dettaglio ${p.title}" loading="lazy"><img src="../../assets/img/optimized/${p.second}" alt="Applicazione ${p.title}" loading="lazy"></section>${nextLink(slug)}`;document.documentElement.style.setProperty("--warm",p.accent);}function nextLink(slug){const order=["bar-giannone","friginfest","amazon-kdp","sicuri-online","menu-design","event-flyer"],next=order[(order.indexOf(slug)+1)%order.length],p=projects[next];return `<a class="next-project" href="${next}-index.html"><span>Progetto successivo</span><strong>${p.title} ↗</strong></a>`;}function year(){document.querySelectorAll("[data-year]").forEach(n=>n.textContent=new Date().getFullYear())}nav();renderCase();year();reveal();})();
